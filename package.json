{
	"name": "slack-notifier",
	"author": "Yevhenii Babichenko <eugene.babichenko@protonmail.com>",
	"description": "Slack notification sender for GitHub Actions",
	"version": "1.0.0",
	"license": "MIT",
	"main": "index.js",
	"dependencies": {
		"@actions/core": "^1.3.0",
		"@slack/webhook": "^6.0.0"
	},
	"devDependencies": {
		"esbuild": "^0.12.15"
	},
	"scripts": {
		"build": "esbuild index.js --outfile=dist/index.js --bundle --minify --external:os --external:path --external:fs"
	}
}
